package mock

import "testing"
import "fmt"

func Test_zigZagConversion_convert(t *testing.T) {
	fmt.Println(convert("AB", 1))
}

func Test_lastRemaining_lastRemaining(t *testing.T) {
	fmt.Println(lastRemaining(12))
}
